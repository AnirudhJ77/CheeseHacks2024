import numpy as np
import pandas as pd


def extract_features(meow):
    '''
    Extracts features from a .wav file

    Parameters:
    meow (file) : vector of features in np array

    Returns
    numpy.ndarray : vector of features
    '''
